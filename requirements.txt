tensorflow
# tensorflow==2.13.0  # Replace with your required version or remove '==' for the latest version
keras==2.13.1       # Usually, Keras is included with TensorFlow, but you can specify it separately if needed
torch==2.1.0        # PyTorch package
torchvision==0.16.0 # Optional: for PyTorch's vision utilities
pillow
pandas
matplotlib
scikit-learn
seaborn